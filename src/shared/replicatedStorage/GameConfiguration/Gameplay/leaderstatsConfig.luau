local LeaderstatsConfig = {}

-- Data keys to show on the leaderboard (these keys must map to numbers in the player's data table).
LeaderstatsConfig.ValuesToDisplay = {
	"Purchases",
}

return LeaderstatsConfig
